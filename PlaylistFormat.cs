using System;

namespace GeneratePlaylist {
	/// <summary>
	/// Playlist format.
	/// </summary>
	public enum PlaylistFormat {
		/// <summary>
		/// The extended M3U format, encoded in UTF-8.
		/// </summary>
		M3U8,
		/// <summary>
		/// The PLS format.
		/// </summary>
		PLS
	}
}
